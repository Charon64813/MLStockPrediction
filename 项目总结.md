# 项目总结：市场状态与机器学习方向预测能力

## 1. 研究问题

> 同一个机器学习模型，在高波动与低波动市场状态下的样本外方向预测能力是否存在显著差异？不同学习范式（线性 / Bagging / Boosting）之间的差异幅度是否不同？

核心度量指标为 `Delta_BA = BA(高波动) − BA(低波动)`，以移动区块 Bootstrap 的 95% 置信区间判断显著性。

## 2. 项目逻辑设计

| 模块 | 设计 |
|---|---|
| 主样本 | SPY 2005—2025 日频；QQQ 作外部验证 |
| 预测目标 | t+1 日开盘→收盘涨跌方向（日内） |
| 特征 | 8 个价格衍生特征（收益、均线偏离、已实现波动、成交量 Z-score） |
| 市场状态 | 20 日已实现波动率 vs 训练集 P70 分位数，按日标记高/低波动 |
| 模型 | 逻辑回归、随机森林、XGBoost + 多数类基线 |
| 训练方式 | 季度扩张窗口（2015—2025 共 44 个测试季度），每年初网格调参 |
| 主指标 | Balanced Accuracy（辅以 ROC-AUC） |

**关键设计原则**：样本外、无未来函数泄漏、三模型统一特征与测试期；重点不在找最准的模型，而在判断预测能力是否具有市场状态依赖性。

## 3. 结果

所有模型 BA / AUC 均在 0.5 附近，无任何 Delta_BA 统计显著（95% CI 全部跨 0）：

| 模型 | All BA | Low Vol | High Vol | Delta_BA |
|---|--:|--:|--:|--:|
| LogisticRegression | 0.497 | 0.500 | 0.488 | −0.011 |
| RandomForest | 0.501 | 0.501 | 0.504 | +0.004 |
| XGBoost | 0.505 | 0.506 | 0.501 | −0.003 |

稳健性检验（8.1 换状态定义、8.2 QQQ 复现、8.3 5 年滚动窗口）结论一致：Delta_BA 均极小且不显著，符号在不同设定间来回翻转。

## 4. 产生结果的原因

**结论：这是一个稳健的「负结果」，根因在研究对象本身，而非工作流缺陷。**

1. **预测目标本身近乎不可预测**：日内 open→close 方向剥离了隔夜信息，是金融文献中信噪比最低的目标之一；标签分布约 54/46，接近抛硬币。
2. **特征无增量信息**：8 个特征均为同一 OHLCV 的简单变换，信息高度重叠，不含外生信号（宏观、跨资产、资金流等）。
3. **模型无法在无信号数据上产生差异**：年度调参在 OOS 无法泛化，三模型 BA 均回归到 0.5 基线，差异被压缩至 ±0.01。
4. **分组样本不平衡削弱功效**：高波动组仅占约 29%，bootstrap CI 较宽，更难显著。

## 5. 研究定位

项目贡献不在于「找到更准的模型」，而在于用一套严格（样本外、无泄漏、多范式、含 bootstrap CI、多稳健性检验）的框架，**实证回答了「方向预测能力是否随市场状态变化」——答案是：在该数据与特征设定下，没有证据支持 regime-dependent predictability，且该负结果本身高度稳健**。这呼应了「方向不可预测、但波动率可预测」这一常见市场不对称现象。
