# 市场状态与机器学习预测项目工作流

## 1. 项目目标

研究高、低波动市场状态是否会系统性影响机器学习模型的股票收益方向预测能力，并比较三种学习范式对市场状态变化的敏感程度：

- 逻辑回归：线性参数模型；
- 随机森林：Bagging 集成模型；
- XGBoost：Boosting 集成模型。

核心研究问题：

> 同一个机器学习模型在高、低波动市场中的样本外方向预测能力是否存在显著差异？三种学习范式的差异程度是否不同？

---

## 2. 最终研究配置

| 模块 | 选择 |
|---|---|
| 主样本 | SPY，2005—2025 年日频数据 |
| 外部验证 | QQQ |
| 预测目标 | 下一交易日开盘到收盘的涨跌方向 |
| 市场状态 | 按日计算的 20 日实现波动率与当前训练期第 70 百分位数；日标签通常会自然聚集为连续状态区间 |
| 模型更新 | 每季度使用扩展训练窗口重新拟合；超参数原则上每年更新一次 |
| 核心模型 | 逻辑回归、随机森林、XGBoost |
| 简单基线 | 始终预测训练集中的多数类别 |
| 主评价指标 | Balanced Accuracy |
| 辅助评价指标 | ROC-AUC |
| 稳定性衡量 | 高、低波动状态的 Balanced Accuracy 差值 |
| 统计检验 | 移动区块 Bootstrap 置信区间 |
| 核心稳健性检验 | 替代状态定义、QQQ 复现、滚动训练窗口 |

---

## 3. 端到端工作流

### 阶段一：锁定实验方案

在查看模型结果前固定以下设计：

1. SPY 为主样本，QQQ 只用于外部复现；
2. 2005—2014 年为初始训练期；
3. 2015—2025 年为样本外测试期；
4. 使用第 t 日及以前的信息预测第 t+1 日；
5. 每季度开始时，使用截至上一季度末的全部真实数据重新拟合模型；
6. 模型超参数原则上每年通过训练期内部的时间序列交叉验证更新一次，季度之间只重新拟合参数，避免重复大规模调参；
7. 所有模型使用完全相同的特征、训练期和测试期；
8. 主指标为 Balanced Accuracy，辅助指标为 ROC-AUC。

这样可以减少看到结果后反复修改实验设计所造成的选择偏差。

### 阶段二：获取并清洗数据

从 Yahoo Finance 下载 SPY 和 QQQ 的复权日频数据：

- Open；
- High；
- Low；
- Close；
- Volume。

数据处理步骤：

1. 按交易日期升序排列；
2. 检查价格和成交量缺失值；
3. 不补齐周末和非交易日；
4. 删除无法计算标签或滚动特征的日期；
5. SPY 与 QQQ 分别生成数据集，不合并训练。

### 阶段三：构造预测标签

主标签定义为：

```text
y(t+1) = 1，若 Close(t+1) > Open(t+1)
y(t+1) = 0，若 Close(t+1) <= Open(t+1)
```

模型使用截至第 t 日收盘时已经获得的信息，预测第 t+1 日开盘到收盘的方向。该定义能够保持预测时点与交易时点一致，降低前视偏差风险。

### 阶段四：构造精简特征

只保留 8 个低冗余特征。

#### 收益特征

1. 1 日收益率；
2. 5 日累计收益率；
3. 20 日累计收益率。

#### 趋势特征

4. 收盘价相对 5 日移动平均线的距离；
5. 收盘价相对 20 日移动平均线的距离。

#### 波动特征

6. 5 日实现波动率；
7. 20 日实现波动率。

#### 交易特征

8. 成交量的 20 日滚动 Z-score。

所有特征只使用第 t 日及以前的数据。逻辑回归需要标准化，标准化器只能在当前训练集上拟合；随机森林和 XGBoost 不强制标准化。

### 阶段五：划分市场状态

用截至第 t 日的 20 日历史收益计算年化实现波动率：

```text
RV(t) = sqrt(252) × Std[r(t-19), ..., r(t)]
```

每个测试季度开始前：

1. 使用截至上一季度末的全部训练数据计算 RV 的第 70 百分位数；
2. 将该阈值固定用于下一个测试季度；
3. 对测试季度中的每个交易日单独计算 RV(t)；
4. RV(t) 高于阈值时将该日标记为高波动，否则标记为低波动。

这里的“按日标记”不意味着市场状态每天毫无规律地切换。由于 RV(t) 使用重叠的 20 日窗口，相邻日期共享 19 天数据，高波动日通常会自然连续出现并形成持续数周或数月的高波动区间。主实验不强制把整个自然季度统一标记为同一种状态，因为这样可能把季度后半段的信息用于季度前半段的状态判断，或者掩盖季度内部真实发生的状态转换。

阶段五只负责为每个日期生成 `regime_t` 标签。三个模型仍使用当时全部历史训练样本统一训练；完成样本外预测后，阶段八再根据 `regime_t` 将预测记录拆成高、低波动两组进行评价。不能使用 2005—2025 年完整样本统一计算阈值，因为这会把测试期信息泄漏到状态定义中。

分别使用高波动样本和低波动样本训练独立模型属于另一种“状态专属模型”研究设计。该方法可以作为扩展分析，但不适合作为本项目主实验：它会丢弃大量训练数据，并将市场状态影响、训练样本量和样本时期差异混在一起。即使强行匹配相同样本量，也会造成信息损失和样本选择问题。

### 阶段六：训练模型

训练四个模型：

1. 多数类别基线；
2. L2 正则化逻辑回归；
3. 随机森林；
4. XGBoost。

采用季度扩展窗口。每个季度开始时，将截至上一季度末已经观察到的真实样本加入训练集并重新拟合模型：

```text
2005—2014 年真实数据训练 → 预测 2015 Q1 每个交易日
2005—2015 Q1 真实数据训练 → 预测 2015 Q2 每个交易日
2005—2015 Q2 真实数据训练 → 预测 2015 Q3 每个交易日
……
截至 2025 Q3 的真实数据训练 → 预测 2025 Q4 每个交易日
```

季度扩展窗口不是递归多步预测。季度内每天都使用当日已经观察到的真实市场特征 X(t)，独立预测下一交易日 y(t+1)。模型预测值不会写回训练集，也不会作为下一天的输入；只有真实结果已经公布后，才会在下一个季度重新训练时加入训练数据，因此不存在由预测值反复传递造成的误差累积。

初始训练期约有 10 年、约 2500 个交易日，对 8 个特征和三个传统机器学习模型已经足够。2015—2025 年共约 44 个测试季度；单个季度约 60 个交易日，季度重训的计算量通常可控。为了避免每季度重复大规模搜索，采用“年度调参、季度拟合”：

- 每年第一季度开始前，在当时训练窗口内部使用 3 折时间序列交叉验证更新一次超参数；
- 当年其余季度沿用该组超参数，只用扩展后的真实数据重新拟合模型；
- 超参数搜索采用小型固定网格；
- 统一使用 Balanced Accuracy 选择参数；
- 不进行随机 K 折交叉验证；
- 不使用当前测试季度参与标准化、特征筛选或调参。

### 阶段七：生成样本外预测结果

保存每个样本外交易日的统一结果表：

| date | asset | regime | y_true | baseline_pred | lr_prob | rf_prob | xgb_prob |
|---|---|---|---:|---:|---:|---:|---:|

该表应成为后续评价和稳健性分析的唯一输入。模型训练完成后，不再根据评价结果反复修改模型。

### 阶段八：进行主结果评价

分别在以下三个样本范围计算模型表现：

1. 全部样本外测试数据；
2. 低波动状态样本；
3. 高波动状态样本。

只保留两个评价指标：

- Balanced Accuracy：主指标；
- ROC-AUC：辅助指标。

模型 m 的状态稳定性定义为：

```text
Delta_BA(m) = BA(m, high) - BA(m, low)
```

解释方式：

- Delta_BA 接近 0：模型在两种状态下相对稳定；
- Delta_BA 明显小于 0：模型在高波动状态下性能下降；
- Delta_BA 大于 0：模型在高波动状态下性能改善。

使用移动区块 Bootstrap 计算 Delta_BA 的 95% 置信区间，以保留金融时间序列的短期相关性。若置信区间不包含 0，则认为状态差异具有统计意义。

### 阶段九：进行精简稳健性检验

只保留三类稳健性检验。

#### 1. 替换市场状态定义

分别使用：

- 20 日实现波动率与第 80 百分位数；
- 60 日实现波动率与第 70 百分位数。

检验主结论是否依赖某个特定窗口或阈值。

#### 2. QQQ 外部复现

使用与 SPY 完全相同的：

- 特征；
- 标签；
- 模型；
- 时间切分；
- 状态定义；
- 评价指标。

检验结论能否从美国大盘市场迁移到科技成长股市场。

#### 3. 替换训练窗口

将扩展训练窗口改为最近 5 年的固定长度滚动窗口，检验主结果是否由训练数据中包含过多陈旧历史规律造成。

---

## 4. 建议删除的内容

### 4.1 删除的特征工程

- RSI、MACD、布林带等大量技术指标；
- 多组高度相关的均线和波动率指标；
- ATR 等额外波动指标；
- PCA 和自动特征选择；
- 宏观、新闻和情绪数据。

这些变量会提高冗余度和处理成本，但不一定能增强市场状态比较的解释力。

### 4.2 删除的评价指标

- 普通 Accuracy；
- Precision、Recall、F1；
- MCC；
- Brier Score 和 Log Loss；
- 多套投资收益、最大回撤、换手率等回测指标。

Balanced Accuracy 与 ROC-AUC 已足够评价二分类方向预测。经济回测可在核心实验全部完成后作为可选扩展，不列为必做内容。

### 4.3 删除的复杂稳定性分析

- PSI、KS 等特征分布漂移检验；
- SHAP 重要性稳定性；
- 低波动转高波动的事件窗口；
- 逐次删除 2008 年、2020 年等危机期；
- 分别使用高波动样本和低波动样本训练独立模型。

其中，分别训练高、低波动模型会把市场状态影响与训练样本量差异混在一起，不适合作为主实验。

---

## 5. 最终结果产出

项目完成后，至少生成以下结果：

1. 一张样本和高低波动状态描述性统计表；
2. 一张三个模型在全样本、高波动和低波动状态下的主结果表；
3. 一张三个模型 Delta_BA 及其 95% 置信区间图；
4. 一张替代市场状态定义的稳健性结果表；
5. 一张 QQQ 外部复现结果表；
6. 一张 5 年滚动训练窗口结果表。

---

## 6. 项目完成标准

满足以下条件即可认为核心实证项目完成：

- 数据处理过程不存在未来信息泄漏；
- 三个模型均通过季度扩展窗口完成 2015—2025 年样本外一步预测；
- 每个测试季度开始前只使用截至上一季度末的真实数据重新拟合模型，预测值不写回训练集；
- 所有模型使用统一特征和统一测试样本；
- 高、低波动状态的 Balanced Accuracy 差值完成统计检验；
- 三类稳健性检验全部完成；
- SPY 主结果与 QQQ 外部验证均有明确结论。

该工作流的重点不是寻找预测准确率最高的模型，而是判断不同学习范式的预测能力是否具有市场状态依赖性。
