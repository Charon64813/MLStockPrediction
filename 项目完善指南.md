# 项目完善指南

本文档用于指导在 `MarketRegime_ML_Prediction.ipynb` 框架基础上完善项目。框架已搭建完毕，下列各项按优先级顺序执行。

---

## 1. 当前框架状态

框架文件 `MarketRegime_ML_Prediction.ipynb` 已包含：

- 完整的 9 阶段 Cell 划分（与 `市场状态与机器学习预测项目工作流.md` 一一对应）
- 所有核心函数定义（`download_data`, `build_features`, `run_expanding_window` 等）
- 全局常量和配置字典
- 评价函数和 Bootstrap CI 函数
- Phase 9 三类稳健性检验的完整实现

**尚需完善**：代码可运行但在细节上需打磨、输出需美化、部分逻辑需验证调整。

---

## 2. 需要完善的具体任务

### 2.1 数据获取与清洗（Phase 2）

**任务**：
- 确保 `yfinance` 下载的数据列名统一为小写（`open`, `high`, `low`, `close`, `volume`）
- 处理 `yfinance` 返回 MultiIndex columns 的情况（当只下载单一 ticker 时有时会出现）
- 在 `check_data_quality()` 函数中增加对日期连续性的检查（非交易日不补齐，但检查是否有意外缺失的交易日）

**可复用 `Final.ipynb` 代码风格**：
```python
# 参考 Final.ipynb Step 1.1 的数据质量检查模式
# 用 print 输出格式化信息，使用 f-string 对齐
print(f'{"Column":20s}: {count:4d} ({count/len(df):.2%})')
```

---

### 2.2 特征工程增强（Phase 4）

**任务**：
- 确认 `build_features()` 中所有特征严格只使用第 t 日及以前的数据，无前视偏差
- 添加特征描述性统计表（参考 Final.ipynb 1.2 节 `df[num_cols].agg(['mean', 'median', 'std']).T.round(2)` 的风格）
- 相关性热力图已在框架中实现，确保在运行后输出正确

**可复用 `Final.ipynb` 代码**：
```python
# 直接复用 Final.ipynb 的相关性热力图代码模板
corr = df[feature_cols].corr()
fig, ax = plt.subplots(figsize=(8, 6))
im = ax.imshow(corr, cmap='RdBu_r', vmin=-1, vmax=1, aspect='auto')
for i in range(len(corr.columns)):
    for j in range(len(corr.columns)):
        ax.text(j, i, f'{corr.iloc[i, j]:.2f}', ha='center', va='center', fontsize=9)
ax.set_xticks(range(len(corr.columns)))
ax.set_xticklabels(corr.columns, rotation=45, ha='right')
ax.set_yticks(range(len(corr.columns)))
ax.set_yticklabels(corr.columns)
plt.colorbar(im, ax=ax)
plt.title('Correlation Heatmap')
plt.tight_layout()
plt.show()
```

---

### 2.3 模型训练与调参（Phase 6）

**任务**：
- `tune_model()` 中对 `LogisticRegression` 需要先标准化再传入，当前框架已处理，验证正确性
- 为 `XGBClassifier` 添加 `use_label_encoder=False, eval_metric='logloss'` 参数避免警告
- 在年度调参时添加 print 输出最佳参数，便于复核
- 确保 `ParameterGrid` 遍历了所有参数组合

**可复用 `Final.ipynb` 代码风格**（模型训练+评价模板）：
```python
# 参考 Final.ipynb Step 4 的模型训练模式
# 训练 → 预测 → 调用 evaluate_model() → 打印结果
model = ModelClass(**params, random_state=RANDOM_STATE)
model.fit(X_train, y_train)
y_pred = model.predict(X_test)
y_prob = model.predict_proba(X_test)[:, 1]
evaluate_model(y_true, y_pred, y_prob, model_name="ModelName")
```

**注意**：`Final.ipynb` 使用了 `RandomizedSearchCV`，但本项目工作流明确要求使用**小型固定网格 + 时间序列交叉验证**，不要用 `RandomizedSearchCV`。框架中已按工作流要求实现手动网格搜索。

---

### 2.4 评价与可视化（Phase 8）

**任务**：
- 主结果表需格式化输出（pivot table 形式，行为模型，列为 scope）
- Delta_BA 条形图需美化：添加 grid、调整字号、标注是否显著
- 增加一个汇总 EDA/Summary Markdown Cell，总结关键发现（参考 Final.ipynb "EDA Summary" 风格）

**可复用 `Final.ipynb` 的 Summary 表格风格**：
```markdown
### Main Results Summary

| Model | All BA | Low Vol BA | High Vol BA | Delta_BA | Significant? |
|-------|--------|-----------|-------------|----------|--------------|
| ...   | ...    | ...       | ...         | ...      | ...          |
```

**可复用的混淆矩阵/结果可视化代码**：
```python
# Final.ipynb 的混淆矩阵热力图模板可适配为 Delta_BA 柱状图
fig, ax = plt.subplots(figsize=(8, 5))
# ... 参见框架中 phase8_plot cell
```

---

### 2.5 稳健性检验完善（Phase 9）

**任务**：
- Phase 9.1：在 `spy` DataFrame 上添加 `rv_60d` 列时，需确保在 `build_features()` 之后、`dropna()` 之前执行，否则会丢失行
- Phase 9.2（QQQ 复现）：确保 QQQ 数据也经过了 `build_features()` 和 `dropna()` 处理
- Phase 9.3（滚动窗口）：`run_rolling_window()` 与 `run_expanding_window()` 结构基本一致，唯一差异是训练窗口的 `train_start` 使用 `train_end - 5years` 而非从最早数据开始
- 所有稳健性检验结果需汇总为一张表输出

**注意**：三类稳健性检验的函数框架都已完成，主要需验证执行流程无误并美化输出。

---

### 2.6 最终产出格式化（Conclusion）

**任务**：生成工作流 md 文档第 5 节要求的 6 张结果：

| # | 产出 | 说明 |
|---|------|------|
| 1 | 描述性统计表 | 样本量、高低波动占比、各状态下 Up 比例 |
| 2 | 主结果表 | 3 模型 × 3 scope 的 BA 和 AUC |
| 3 | Delta_BA + 95% CI 图 | 条形图 + 误差线 |
| 4 | 替代状态定义稳健性表 | RV20P80 和 RV60P70 的 Delta_BA |
| 5 | QQQ 外部复现结果表 | 与 SPY 主结果表结构一致 |
| 6 | 5 年滚动窗口结果表 | 与主结果表结构一致 |

每张表/图用独立的 Cell + 上方 Markdown 标题标注。

---

## 3. 代码风格要求

严格遵循 `Final.ipynb` 的风格：

| 要素 | 规范 |
|------|------|
| Markdown 标题 | `## Phase N: Title` 大标题，`### N.x Subtitle` 子标题 |
| 注释语言 | 英文简洁注释 |
| 变量命名 | 小写 + 下划线：`spy_results`, `X_train`, `y_test` |
| 常量命名 | 全大写：`RANDOM_STATE`, `FEATURE_COLS` |
| 函数文档 | 三引号 docstring，一句话描述功能 |
| 输出验证 | 每步骤结尾用 `print()` 或 `.head()` 验证中间结果 |
| 警告抑制 | 文件开头 `warnings.filterwarnings('ignore')` |
| 图表 | `plt.tight_layout()` + `plt.show()` |

---

## 4. 关键注意事项

### 4.1 避免前视偏差
- **波动率阈值**：每季度的 RV percentile 只能用截至上一季度末的训练数据计算
- **标准化器**：只在训练集上 `fit`，测试集上 `transform`
- **特征构造**：所有滚动窗口特征只用当日及之前数据

### 4.2 季度时间切分
- 使用 `pd.date_range(start, end, freq='QS')` 生成季度起始日
- 训练数据：`df.index <= q_start - 1 day`
- 测试数据：`q_start <= df.index <= q_end`

### 4.3 Bootstrap 实现
- 使用**移动区块 Bootstrap**（block_size=20），保留金融时间序列短期相关性
- 若 boot 样本中高/低波动样本 < 5，跳过该次抽样
- 报告 95% CI，若 CI 不包含 0 则标记 `*` 表示统计显著

---

## 5. 执行顺序建议

1. **先跑通主实验**：确保 SPY 的 Phase 2→8 能完整运行并输出结果
2. **再完善稳健性**：Phase 9 的三类检验
3. **最后美化输出**：格式化表格、图表、Summary Cell
4. **全量运行一次**：确认所有 Cell 按顺序执行无报错

---

## 6. 依赖包版本建议

```
python >= 3.11
pandas >= 2.0
numpy >= 1.24
matplotlib >= 3.7
scikit-learn >= 1.3
xgboost >= 2.0
yfinance >= 0.2.30
```

可在 notebook 第一个 Cell 通过 `!pip install` 安装或通过 `requirements.txt` 管理。
